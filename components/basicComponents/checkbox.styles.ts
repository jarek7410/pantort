import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    buttonText: {
        fontSize: 20,
    },
    text:{
        fontSize:20,
        color:"black",
    },
})
export default styles;